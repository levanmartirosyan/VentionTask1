services:
  api:
    image: ventiontask1-api
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ventiontask1-api-compose
    ports:
      - "8081:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
